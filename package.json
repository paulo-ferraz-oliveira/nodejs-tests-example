{
  "name": "nodejs-tests-example",
  "license": "MIT",
  "private": true,
  "scripts": {
    "format": "prettier src/**/*.js --write && prettier test/**/*.js --write && prettier *.mjs --write && prettier package.json --write",
    "lint": "eslint src/**/*.js && eslint test/**/*.js",
    "format-lint": "npm install && npm run format && npm run lint",
    "test": "node test/pubsub.test.js"
  },
  "dependencies": {
    "nodejs-tests": "github:paulo-ferraz-oliveira/nodejs-tests#0.0.6"
  },
  "devDependencies": {
    "globals": "17.12.0",
    "@eslint/js": "10.0.1",
    "eslint": "10.10.0",
    "markdownlint-cli": "0.49.1",
    "prettier": "3.9.6",
    "yaml-lint": "1.7.0"
  },
  "engines": {
    "node": "24.21.0"
  }
}
